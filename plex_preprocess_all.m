function finalname = plex_preprocess_all% function finalname = plex_preprocess_all%% (formerly called plex_merge_all)% Calls the plex_preprocess function repeatedly to process the .mat data files in the current directory.% Plexon data is assumed to be in a central repository.%% First, get list of cells in this directory:finalname = dirgetcells;% Ok, now call plex_merge_dir for each cell.%for i = 1:length(finalname)	plex_preprocess(finalname{i});	fprintf('\n');end